import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"DOU DersAktif — Sınıfı oyuna dahil et",description:"Canlı quizler, anketler ve takım yarışlarıyla öğrencileri derse dahil et.",other:{"codex-preview":"development"}};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="tr"><body>{children}</body></html>}
